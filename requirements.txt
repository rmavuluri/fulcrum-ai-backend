flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.1.0
certifi>=2024.0.0
requests>=2.31.0

# Optional: only if using Claude + MCP for chat (when USE_ALLY_SANDBOX is not set)
# mcp[cli]>=1.8.0
# anthropic>=0.51.0
# prompt-toolkit>=3.0.51
